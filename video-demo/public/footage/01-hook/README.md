# 01-hook 開場素材（雜亂廚房蒙太奇）

請將 6 張圖片複製到此目錄，並重命名為：

- `01.png`、`02.png`、…、`06.png`（按播放順序：訂單小票 → 手寫庫存單 → 冰箱 → 儲藏架 → 菜單表 → 手寫訂單表）

共 6 張圖，每張顯示 1 秒，共 6 秒。文案："Too much paper, too much waste, too much chaos—it doesn't have to be."

若使用 `.jpg`，請在 `src/compositions/01-HookScene.tsx` 中將 `HOOK_IMAGES` 改為 `.jpg`。若張數不同，可修改該陣列。
