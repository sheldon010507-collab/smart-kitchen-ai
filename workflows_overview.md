# AI Kitchen 核心工作流 (Core Workflows)

基于已实现的功能模块，以下是 AI Kitchen 支持的几个核心业务工作流。这些流程展示了系统如何帮助餐厅从采购、管理到销售的各个环节进行闭环管理。

## 1. 智能采购入库流程 (Procurement to Stock Workflow)
这是利用 AI 扫描功能实现库存快速更新的核心流程。

1.  **采购/收货**: 员工或经理完成原料采购。
2.  **AI 扫描**:
    *   用户在应用中打开 **Scanner**。
    *   选择 **Receipt Mode (收据模式)** 并拍摄购物小票。
    *   **或者**选择 **Fridge Mode (冰箱模式)** 直接拍摄刚放入冰箱的食材。
3.  **AI 解析与校对**:
    *   系统利用大模型自动识别收据上的物品名称、数量、单价和总价。
    *   用户在 **Review Screen (校对页)** 检查识别结果，修正任何错误，并为新物品指定分类 (如: 蔬菜) 和存放位置 (如: 冷藏柜)。
4.  **一键入库**: 确认无误后，点击保存。系统自动将所有物品添加到 **Inventory (库存)** 中，包括其有效期和单位成本。

## 2. 菜单研发与成本定价流程 (Menu Engineering Workflow)
帮助餐厅控制食品成本 (Food Cost) 并合理定价的流程。

1.  **创建菜品**: 厨师或经理在 **Menu Manager** 中新建一道菜 (例如: "经典牛肉汉堡")。
2.  **定义 BOM (配方)**:
    *   在菜品详情中添加 **Ingredients (食材)**。
    *   从库存中选择对应原料 (如: 牛肉饼、生菜、面包胚)。
    *   输入每份用量 (如: 牛肉 200g, 生菜 50g)。
3.  **自动核算成本**:
    *   系统根据库存中记录的原料 **单位成本**，自动计算出一份汉堡的 **Total Cost (总成本)**。
4.  **定价决策**:
    *   输入 **Selling Price (售价)**。
    *   系统即时显示 **Margin (毛利率)**。如果属于低利润菜品，系统可能会给出视觉提示，帮助管理者调整售价或优化配方。

## 3. 员工管理与排班流程 (Staff Management & Scheduling)
从员工入职到日常排班的管理闭环。

1.  **店铺创建与邀请**:
    *   经理创建店铺，生成唯一的 **Join Code**。
2.  **员工入职**:
    *   新员工注册账号，输入 Join Code 申请加入。
    *   经理在 **Staff List** 中看到 "Pending" 的申请，点击 **Approve** 批准入职，并设置员工的角色 (Chef/Server) 和时薪。
3.  **排班制定**:
    *   经理进入 **Calendar** 视图。
    *   点击日期添加 **Shift (班次)**，指定员工、开始/结束时间。
4.  **成本预估**:
    *   排班完成后，系统根据该员工的时薪自动计算该班次的 **Labor Cost (人力成本)**，经理可直观看到当天的预计人工支出。

## 4. 日常后厨运营流程 (Daily Kitchen Operations)
厨师团队的日常工作流。

1.  **查看备货清单**:
    *   上班后，厨师打开 **Dashboard** 或 **Prep List**。
    *   查看当天需要完成的备料任务 (例如: "切洋葱 5kg", "制作酱汁")。
2.  **执行与打钩**:
    *   完成一项任务后，在系统中勾选完成。
3.  **查看食谱 (Chef View)**:
    *   在制作菜品时，切换到 **Chef View** 查看标准化的食谱步骤，确保出品质量一致。
4.  **库存消耗 (销售录入)**:
    *   前厅完成销售后，可以通过 **Sales Scanner** 扫描小票。
    *   (未来扩展功能: 系统根据销售的菜品，自动扣减对应的库存原料数量)。

## 5. 多店管理流程 (Multi-Location Management)
适用于连锁店老板的流程。

1.  **切换店铺**: 在应用顶部菜单快速切换不同的 **Business ID**。
2.  **独立数据查看**: 切换后，库存、人员、菜单和财务数据完全隔离，管理者可以分别查看A店和B店的运营状况。
