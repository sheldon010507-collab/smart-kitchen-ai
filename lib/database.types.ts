// Re-export types from the main types file for database layer compatibility
export type { Business, InventoryItem, User, Recipe, PrepTask, SalesItem, SalesReceipt, Staff, Shift, MenuItem, IngredientUsage } from '../types';
